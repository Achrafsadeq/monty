>,>,<           # Get two numbers (in ASCII)
[>+<-]>          # Add the two ASCII values together
<<++++[>++++++++++<-]++[>++++<-]>  # Set up to get the ASCII code for '0' (ASCII 48)
[>-<-]>.         # Subtract 48 from the ASCII sum to get the result as a digit, then print it.
